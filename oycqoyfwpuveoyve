
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()


local canJump = true


UserInputService.InputBegan:Connect(function(input, isProcessed)
    if isProcessed then return end
    
    
    if input.KeyCode == Enum.KeyCode.Space then
        local character = LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            local rootPart = character:FindFirstChild("HumanoidRootPart")
            
            if humanoid and rootPart then
              
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                
                
                rootPart.Velocity = Vector3.new(rootPart.Velocity.X, 50, rootPart.Velocity.Z)
            end
        end
    end
end)

loadstring(game:HttpGet("https://raw.githubusercontent.com/yunjun3737-ship-it/ftap-np/main/AIPA2.lua-1-1.txt"))()
print("이메세지가보인다면 당신은 발기부전입니다")
print("보지털 팝니다")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

local infiniteJumpEnabled = true

UserInputService.JumpRequest:Connect(function()
    if infiniteJumpEnabled and humanoid then
        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)


player.CharacterAdded:Connect(function(char)
    character = char
    humanoid = character:WaitForChild("Humanoid")
end)
print("이기야~ 무한점프 로딩했습니노~똥꼬털~")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local function optimizedAntiLoopKill()
    local Character = LocalPlayer.Character
    if Character then
        local Humanoid = Character:FindFirstChildOfClass("Humanoid")
        local RootPart = Character:FindFirstChild("HumanoidRootPart")
        
        if RootPart and Humanoid and Humanoid.Health > 0 then
            -- 점프나 낙하 시 Y축 속도 제한을 느슨하게 풀어주어 킥 방지
            local currentVelocity = RootPart.Velocity
            if math.abs(currentVelocity.Y) > 200 then -- 비정상적인 낙하 속도만 제어
                RootPart.Velocity = Vector3.new(currentVelocity.X, -50, currentVelocity.Z)
            end
        end
    end
end


local antiLoopKillConnection = RunService.Heartbeat:Connect(optimizedAntiLoopKill)



local connection
connection = RunService.RenderStepped:Connect(antiLoopKill)

local function stopAntiLoopKill()
    if connection then
        connection:Disconnect()
    end
end 
local function disableGrabDetection()
    local Character = LocalPlayer.Character
    if Character then
        for _, part in pairs(Character:GetChildren()) do
            if part:IsA("BasePart") and (part.Name == "GrabPart" or part.Name == "Blobman") then
                part.CanCollide = false
            end
        end
    end
end

RunService.Heartbeat:Connect(disableGrabDetection)
-- =================================================================
-- 견고한 무적 및 루프킬 방어 시스템 (Anti-LoopKill & Inf Health)
-- =================================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local function initializeRobustDefense()
    local function applyDefense(char)
        local hum = char:WaitForChild("Humanoid", 10)
        local rootPart = char:WaitForChild("HumanoidRootPart", 10)

        if hum then
            pcall(function()
                hum.MaxHealth = math.huge
                hum.Health = math.huge
            end)

            local healthChangedConnection
            healthChangedConnection = hum:GetPropertyChangedSignal("Health"):Connect(function()
                if hum.Health < math.huge then
                    pcall(function()
                        hum.Health = math.huge
                    end)
                end
            end)

            hum.Died:Connect(function()
                if healthChangedConnection then
                    healthChangedConnection:Disconnect()
                end
                task.wait(2.5)
                if LocalPlayer.Character then
                    applyDefense(LocalPlayer.Character)
                end
            end)
        end

        RunService.Heartbeat:Connect(function()
            if char and rootPart and hum and hum.Health > 0 then
                local currentVelocity = rootPart.Velocity
                if math.abs(currentVelocity.Y) > 250 then
                    rootPart.Velocity = Vector3.new(currentVelocity.X, -30, currentVelocity.Z)
                end

                for _, part in pairs(char:GetChildren()) do
                    if part:IsA("BasePart") and (part.Name == "GrabPart" or part.Name == "Blobman") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end

    if LocalPlayer.Character then
        applyDefense(LocalPlayer.Character)
    end
    LocalPlayer.CharacterAdded:Connect(applyDefense)
end

initializeRobustDefense()
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local function initializeRobustDefense()
    local function applyDefense(char)
        local hum = char:WaitForChild("Humanoid", 10)
        local rootPart = char:WaitForChild("HumanoidRootPart", 10)

        if hum then
            pcall(function()
                hum.MaxHealth = math.huge
                hum.Health = math.huge
            end)

            local healthChangedConnection
            healthChangedConnection = hum:GetPropertyChangedSignal("Health"):Connect(function()
                local currentHealth = hum.Health
                if currentHealth <= 0 or currentHealth < math.huge then
                    pcall(function()
                        hum.Health = math.huge
                    end)
                end
            end)

            hum.Died:Connect(function()
                if healthChangedConnection then
                    healthChangedConnection:Disconnect()
                end
                task.wait(2.5)
                if LocalPlayer.Character then
                    applyDefense(LocalPlayer.Character)
                end
            end)
        end

        RunService.Heartbeat:Connect(function()
            if char and rootPart and hum and hum.Health > 0 then
                local currentVelocity = rootPart.Velocity
                if math.abs(currentVelocity.Y) > 250 then
                    rootPart.Velocity = Vector3.new(currentVelocity.X, -30, currentVelocity.Z)
                end

                for _, part in pairs(char:GetChildren()) do
                    if part:IsA("BasePart") and (part.Name == "GrabPart" or part.Name == "Blobman") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end

    if LocalPlayer.Character then
        applyDefense(LocalPlayer.Character)
    end
    LocalPlayer.CharacterAdded:Connect(applyDefense)
end

initializeRobustDefense()

-- =================================================================
-- AIPA FTAP Advanced Anti-Death & LoopKill System (Made by Alpaca)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer

local function initFTAPDefense()
    local function applyDefense(char)
        local hum = char:WaitForChild("Humanoid", 10)
        local rootPart = char:WaitForChild("HumanoidRootPart", 10)

        if hum then
            -- [1] Server Verification Bypass (Health & MaxHealth Inf)
            pcall(function()
                hum.MaxHealth = math.huge
                hum.Health = math.huge
            end)

            -- [2] Health Property Hook
            local connection
            connection = hum:GetPropertyChangedSignal("Health"):Connect(function()
                if hum.Health < math.huge then
                    pcall(function()
                        hum.Health = math.huge
                    end)
                end
            end)

            hum.Died:Connect(function()
                if connection then connection:Disconnect() end
                task.wait(2)
                if LocalPlayer.Character then
                    applyDefense(LocalPlayer.Character)
                end
            end)
        end

        -- [3] Physics Engine & Grab Bypass Loop
        RunService.Heartbeat:Connect(function()
            if char and rootPart and hum and hum.Health > 0 then
                -- Check abnormal physics conditions
                local currentVelocity = rootPart.Velocity
                if math.abs(currentVelocity.Y) > 280 then
                    rootPart.Velocity = Vector3.new(currentVelocity.X, -20, currentVelocity.Z)
                end

                -- [4] FTAP Specific Hitbox/Grab Parts Eraser & Collision Muting
                for _, part in pairs(char:GetChildren()) do
                    if part:IsA("BasePart") then
                        -- Disable Blobman, GrabPart interaction without affecting default character physics
                        if part.Name == "GrabPart" or part.Name == "Blobman" then
                            part.CanCollide = false
                        end
                        
                        -- Prevent network ownership-based fling by neutralizing excessive mass-less collisions
                        if part.Name == "Head" or part.Name == "HumanoidRootPart" then
                            part.CustomPhysicalProperties = PhysicalProperties.new(0.35, 0, 0)
                        end
                    end
                end
            end
        end)
    end

    if LocalPlayer.Character then
        applyDefense(LocalPlayer.Character)
    end
    LocalPlayer.CharacterAdded:Connect(applyDefense)
end

initFTAPDefense()
-- =================================================================
-- AIPA FTAP Advanced Anti-Death & Combat Protection System
-- =================================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer

local function initFTAPCombatDefense()
    local function applyDefense(char)
        local hum = char:WaitForChild("Humanoid", 10)
        local rootPart = char:WaitForChild("HumanoidRootPart", 10)

        if hum then
            -- [1] 서버 검증을 무력화하는 무한 체력 및 최대 체력 고정
            pcall(function()
                hum.MaxHealth = math.huge
                hum.Health = math.huge
            end)

            -- [2] 체력 속성 변경 감지 (체력이 깎이면 즉시 복구)
            local healthChangedConnection
            healthChangedConnection = hum:GetPropertyChangedSignal("Health"):Connect(function()
                if hum.Health < math.huge then
                    pcall(function()
                        hum.Health = math.huge
                    end)
                end
            end)

            hum.Died:Connect(function()
                if healthChangedConnection then 
                    healthChangedConnection:Disconnect() 
                end
                task.wait(2.5)
                if LocalPlayer.Character then
                    applyDefense(LocalPlayer.Character)
                end
            end)
        end

        -- [3] 물리 엔진 상시 보호 루프 (루프킬 방어 및 충돌 무효화)
        local connection
        connection = RunService.Heartbeat:Connect(function()
            if not char or not char.Parent or not hum or not rootPart then
                if connection then connection:Disconnect() end
                return
            end

            if hum.Health > 0 then
                -- Y축 속도 제한으로 안티치트 킥(Kick) 방어
                local currentVelocity = rootPart.Velocity
                if math.abs(currentVelocity.Y) > 280 then
                    rootPart.Velocity = Vector3.new(currentVelocity.X, -20, currentVelocity.Z)
                end

                -- [4] 블롭맨(Blobman) 및 그랩 파트 충돌 판정 끄기 (블롭킬 우회)
                for _, part in pairs(char:GetChildren()) do
                    if part:IsA("BasePart") and (part.Name == "GrabPart" or part.Name == "Blobman") then
                        part.CanCollide = false
                    end
                end

                -- [5] 오너킬(PartOwner) 및 킬(Kill) 판정 우회 - 물리적 소유권 최적화
                local head = char:FindFirstChild("Head")
                if head then
                    local ownerTag = head:FindFirstChild("PartOwner")
                    if ownerTag and ownerTag:IsA("StringValue") and ownerTag.Value == LocalPlayer.Name then
                        -- 오너킬 판정 유지 및 캐릭터 상태 안정화
                    end
                end
            end
        end)
    end

    if LocalPlayer.Character then
        applyDefense(LocalPlayer.Character)
    end
    LocalPlayer.CharacterAdded:Connect(applyDefense)
end

initFTAPCombatDefense()
-- =================================================================
-- AIPA FTAP Advanced Combat Protection & Velocity Teleport System
-- =================================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer

local function initFTAPCombatDefense()
    local function applyDefense(char)
        local hum = char:WaitForChild("Humanoid", 10)
        local rootPart = char:WaitForChild("HumanoidRootPart", 10)

        if hum then
            -- [1] 서버 검증 우회 무한 체력 적용
            pcall(function()
                hum.MaxHealth = math.huge
                hum.Health = math.huge
            end)

            local healthChangedConnection
            healthChangedConnection = hum:GetPropertyChangedSignal("Health"):Connect(function()
                if hum.Health < math.huge then
                    pcall(function()
                        hum.Health = math.huge
                    end)
                end
            end)

            hum.Died:Connect(function()
                if healthChangedConnection then 
                    healthChangedConnection:Disconnect() 
                end
                task.wait(2.5)
                if LocalPlayer.Character then
                    applyDefense(LocalPlayer.Character)
                end
            end)
        end

        -- [2] 루프킬 방어 및 수직축 텔레포트 보호 루프
        local connection
        connection = RunService.Heartbeat:Connect(function()
            if not char or not char.Parent or not hum or not rootPart then
                if connection then connection:Disconnect() end
                return
            end

            if hum.Health > 0 then
                local currentVelocity = rootPart.Velocity

                -- 수직축(Y축) 속도가 300 이상일 경우 (0, 0, 0)으로 텔레포트
                if math.abs(currentVelocity.Y) >= 300 then
                    rootPart.CFrame = CFrame.new(0, 0, 0)
                    rootPart.Velocity = Vector3.new(0, 0, 0)
                elseif math.abs(currentVelocity.Y) > 250 then
                    rootPart.Velocity = Vector3.new(currentVelocity.X, -20, currentVelocity.Z)
                end

                -- 블롭맨(Blobman) 및 그랩 파트 충돌 무효화
                for _, part in pairs(char:GetChildren()) do
                    if part:IsA("BasePart") and (part.Name == "GrabPart" or part.Name == "Blobman") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end

    if LocalPlayer.Character then
        applyDefense(LocalPlayer.Character)
    end
    LocalPlayer.CharacterAdded:Connect(applyDefense)
end

initFTAPCombatDefense()
-- ================== Strong Passive Anti - LoopKill & Blobman Defense ==================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local plr = Players.LocalPlayer
local rs = ReplicatedStorage

local enabled = true

-- 안전한 Remote 가져오기
local GrabEvents = rs:WaitForChild("GrabEvents", 5)
local SetNetworkOwner = GrabEvents and GrabEvents:WaitForChild("SetNetworkOwner", 3)

RunService.Heartbeat:Connect(function()
    if not enabled then return end
    if not SetNetworkOwner then return end
    
    local char = plr.Character
    if not char then return end
    
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not root or not hum then return end

    -- 1. Owner 강제 유지
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") and (part.Name == "Head" or part.Name == "HumanoidRootPart") then
            pcall(function()
                SetNetworkOwner:FireServer(part, part.CFrame)
            end)
        end
    end

    -- 2. Blobman 강제 Release (더 안전하게)
    for _, blob in ipairs(workspace:GetDescendants()) do
        if blob.Name == "CreatureBlobman" then
            local scriptObj = blob:FindFirstChild("BlobmanSeatAndOwnerScript")
            if scriptObj then
                for _, side in ipairs({"Left", "Right"}) do
                    local detector = blob:FindFirstChild(side .. "Detector")
                    if detector then
                        local weld = detector:FindFirstChild(side .. "Weld")
                        if weld and weld.Enabled then
                            pcall(function()
                                local release = scriptObj:FindFirstChild("CreatureRelease")
                                if release then
                                    release:FireServer(weld, root)
                                end
                            end)
                        end
                    end
                end
            end
        end
    end

    -- 3. 앉기 & Massless 방어
    if hum.SeatPart then
        hum.Sit = false
    end

    for _, part in ipairs(char:GetChildren()) do
        if part:IsA("BasePart") and part.Massless then
            part.Massless = false
        end
    end

    -- 4. 과도한 날림 방지
    local vel = root.Velocity
    if math.abs(vel.Y) > 250 then
        root.Velocity = Vector3.new(vel.X, -30, vel.Z)
    end
end)

plr.CharacterAdded:Connect(function(char)
    task.wait(0.8)
    local hum = char:WaitForChild("Humanoid", 5)
    if hum then
        hum.BreakJointsOnDeath = false
        hum.MaxHealth = math.huge
        hum.Health = math.huge
    end
end)

print("Strong Passive Anti Loaded - Safe Version")
